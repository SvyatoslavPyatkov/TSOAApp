const dbConfig = {
    HOST: "localhost",
    USER: "root",
    PASSWORD: "root",
    DB: "db_learner_records",
    dialect: "mysql",
    define: {
        timestamps: false
    }
};

export default dbConfig;